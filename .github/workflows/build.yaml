name: Publish Maven Artifact

on:
  push:
    branches: [ "master" ]
    paths:
      - 'src/**'
      - 'build.gradle'
      - 'settings.gradle'
      - 'gradle/**'

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the damn repo (with full history)
        uses: actions/checkout@v3
        with:
          fetch-depth: 1000

      - name: Set up Java (because apparently you can’t manage without it)
        uses: actions/setup-java@v3
        with:
          distribution: corretto
          java-version: '24'  # Change this if you’re too much of a wimp for another version
          cache: gradle

      - name: Make gradlew executable (try not to screw it up)
        run: chmod +x gradlew

      - name: Build and Publish to Maven (get your act together)
        run: ./gradlew clean publish
        env:
          MAVEN_USER: ${{ secrets.MAVEN_USER }}
          MAVEN_PASSWORD: ${{ secrets.MAVEN_PASSWORD }}
